{
  "name": "@draftpkg/action",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/main.mjs",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@draftpkg/config": "workspace:*",
    "@octokit/rest": "^22.0.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "memfs": "^4.56.11",
    "typescript": "^5.9.2",
    "vitest": "^3.2.1"
  }
}
